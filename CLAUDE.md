# FC풉살 드래프트 — 작업 가이드

여자 풋살팀 FC풉살(총 23명 — 참석 20 · 불참 3)의 하반기 내부매치 팀을 나누는 웹앱.
불참자도 투표·각오·캐릭터는 똑같이 하고, 드래프트도 스네이크 순번 그대로 참석 여부와
무관하게 아무 때나 뽑는다 — 참석 여부로 지명을 막지 않는다(`draft_players.attending` 은
코치 화면 라벨용 참고 정보일 뿐, `coach_pick`은 이 값을 아예 보지 않는다). 처음엔
"참석자를 다 뽑은 뒤 불참자를 이어서" 뽑는 관행으로 진행했지만 강제된 순서는 아니었고,
한 팀의 요청으로 그 관행도 접었다 — 이제 정말 아무 순서로나 뽑는다.
**선수 무기명 투표 → 코치 전용 드래프트 → 지명 순번을 지운 결과 공개 → 예측·응원 → 매치데이(12/19)**
6단계가 `draft_config.phase` 값 하나로 굴러간다.

상세 설계는 [`PLAN.md`](PLAN.md), 운영 절차는 [`docs/OPERATIONS.md`](docs/OPERATIONS.md),
선수·코치·마스터에게 보낼 카톡 안내문과 링크는 [`docs/NOTICES.md`](docs/NOTICES.md).

## 실행

빌드 도구 없음. 정적 HTML/CSS/JS라 그냥 서버만 띄우면 된다.

```bash
python3 -m http.server 8099
```

`?demo=1` 을 붙이면 **네트워크 없이** 가짜 데이터로 전 화면이 돌아간다. 디자인 작업은 여기서 하면 된다.

```
index.html?demo=1&phase=VOTE      투표 (+ #내캐릭터 커스터마이저)
index.html?demo=1&phase=REVEAL    프듀101식 발표
index.html?demo=1&phase=DRAFT     선수 대기 화면
index.html?demo=1&phase=RESULT    팀 명단
index.html?demo=1&phase=PREDICT   예측 · 승률 · 응원
index.html?demo=1&phase=MATCH     경기 결과 · 순위표 · 적중자
coach.html?demo=1&phase=DRAFT     코치 드래프트 보드 (암호 아무거나)
admin.html?demo=1&phase=VOTE      마스터 콘솔 (암호 아무거나)
```

## 구조

| 파일 | 역할 |
|---|---|
| `index.html` | 선수 23명용. 슬랙식 채널(#투표 #내캐릭터 #결과발표 #드래프트 #우리팀 #예측 #응원 #경기)로 단계 전환 |
| `coach.html` | 코치 6명용 드래프트 보드 (암호 게이트) |
| `admin.html` | 마스터용 운영 콘솔 (암호 게이트) |
| `assets/config.js` | Supabase URL/공개키, 팀 컬러, 에러 메시지 |
| `assets/app.js` | API 레이어 + 데모 모드 + 공통 유틸 |
| `assets/character.js` | 캐릭터 SVG 생성 — 저장된 룩이 있으면 그걸, 없으면 이름 해시로 |
| `assets/style.css` | 디자인 시스템 전부 |
| `assets/fonts/` | Pretendard 서브셋 (직접 포함) |

## 디자인 현황

- **폰트**: `--font` = `-apple-system → Apple SD Gothic Neo → Pretendard`.
  애플 기기는 시스템 폰트, 그 외는 저장소에 넣어둔 Pretendard.
  ⚠️ 폰트를 CDN(jsDelivr·Google Fonts)에서 불러오지 말 것 — 일부 환경에서 차단돼
  맑은 고딕으로 떨어졌던 전력이 있다. 웹폰트가 필요하면 `assets/fonts/`에 직접 넣는다.
  ⚠️ 폰트뿐 아니라 **브라우저로 나가는 모든 리소스**가 저장소 안에 있어야 한다.
  축포도 같은 이유로 CDN 라이브러리를 걷어내고 `app.js` 안 캔버스 구현으로 바꿨다.
  외부 `<script src>` · `<link href>` 를 새로 추가하지 말 것.
- **컬러**: 오버진 퍼플 계열 셸 + 팀 3색 (A `#36C5F0` / B `#E01E5A` / C `#2EB67D`).
- **형태**: 라운드 크게(카드 22px, 버튼 pill), 눌리는 입체 버튼.
- **캐릭터**: 플랫 지오메트릭 축구 선수. **21가지**를 직접 고른다 —
  자세 · 키 · **체형** · 머리(길이/스타일/색) · **수염** · 피부색 · 표정 ·
  상의(무늬/색) · **등번호(0~999 직접 입력)** · 하의(무늬/색) · 양말(무늬/색) ·
  신발색 · 액세서리(+소품색) · 테두리 · 배경색.
  전부 팔레트 인덱스라 jsonb 한 줄로 저장된다 (조합 약 1.0×10²¹).
  체형(`BUILD`)은 어깨 너비·팔 두께·팔 위치를 바꾼다. 몸통은 사각형이 아니라
  어깨에서 넓어져 허리로 좁아지는 **사다리꼴 path** 이고, 무늬 클립도 같은 path 를 쓴다.
  무늬는 몸통보다 넉넉히 그려 클립으로 자른다 — 딱 맞게 그리면 넓은 어깨가 민무늬로 남는다.
  수염·체형은 성별을 나누는 항목이 아니라 누구나 고르는 한 칸이다.
  ⚠️ `SIZE` 를 바꾸면 **DB 쪽 `v_max` 도 같이** 바꿔야 한다 —
  `submit_look`(선수)·`coach_set_card`(코치) 두 함수에 있고, 서버가 값을 자르는 유일한 기준이다.
  안 맞추면 새 값이 조용히 버려져 "저장했는데 안 바뀌네"가 된다.
  키는 0~4 가 viewBox 안에 들어오고(발은 나란히, 머리 높이만 달라짐) 마지막 칸만
  프레임을 뚫는다. 인접 단계가 10% 넘게 벌어져야 작은 명단에서도 차이가 보인다.
  팔은 몸통 **앞에** 그린다 — 팔짱·박수처럼 가슴 앞으로 접는 자세에서 가려지기 때문.
  자세는 팔다리를 어깨·팔꿈치·엉덩이·무릎 네 관절에서 회전시켜 만든다 (`POSE` 표).
  리프팅·슈팅은 공을, 하트는 하트를 소품으로 함께 그린다. 새 자세를 추가하려면
  각도 4+4개와 소품 좌표만 적으면 된다.
  `charSvg(name, { size, jersey, number, bounce, look, crop })`.
  저장된 룩은 `FDChar.setLooks(map)` 으로 한 번 등록하면 이후 모든 호출에 자동 반영되므로,
  화면마다 룩을 넘겨줄 필요가 없다. 편집 미리보기는 `crop:false` 로 머리 잘림을 끈다.
  룩이 없으면 이름 해시로 만든다 — 같은 이름은 항상 같은 캐릭터.
  `TALL` 맵에 이름을 넣으면 기본 키가 커진다 (다이 = 가장 큰 칸, 머리가 프레임 위로 잘림).
- **커스터마이저**: 미리보기는 `.stage-stick` 으로 화면에 고정된다 (컨트롤이 21줄이라
  스크롤하면 캐릭터가 시야에서 사라진다). `position: sticky` 는 **부모 박스 안에서만**
  붙으므로 미리보기를 카드 안에 두면 안 되고, 스크롤 구간 전체를 덮는 `.look-editor`
  래퍼의 직계 자식이어야 한다. 붙는 위치는 `--sticky-top`(모바일 topbar 높이,
  `syncStickyTop()` 이 잰다)으로 맞춘다.
- **대기장**(`loungeHtml`): 읽는 일은 위쪽 **스포트라이트**가 전담한다 (84px 캐릭터 +
  15px 각오, 3.2초마다 교대). 아래 6열 명단은 "누가 왔나"만 본다 — 각오를 18칸에
  나눠 담으면 전부 작아져 아무것도 안 읽힌다. 명단의 누구든 누르면 그 사람이
  스포트라이트에 서고 9초간 자동 교대가 멈춘다. 스포트라이트는 `crop:false` 라
  키 큰 캐릭터의 머리가 위로 넘치므로 `.stage` 위쪽 44px 을 비워둔다.
- **모션**: `.chr-bounce` — 두 박자 둠칫둠칫. 착지에서 눌리고 뜰 때 늘어나는
  스쿼시&스트레치로 리듬을 만든다. `prefers-reduced-motion` 에서는 꺼진다.

## 카카오톡 인앱 브라우저 대응

링크는 카톡 채팅으로 공유되므로 대부분 카톡 인앱 브라우저로 열린다. 이 브라우저는
렌더링·키보드 상호작용이 특히 불안정해서 두 가지를 대응해 둔다.

- **탈출 배너** (`kakaoExitBannerHtml()` in `app.js`): `navigator.userAgent` 에
  `KAKAOTALK` 이 잡히면 상단(`#kakaoExit`/`#kakaoExitGate`)에 "브라우저로 열기" 배너를
  띄운다. 탭하면 `kakaotalk://web/openExternal?url=...` 스킴으로 기본 브라우저를 연다.
  ⚠️ **자동으로는 못 연다** — 페이지 로드 시점에 스크립트로 바로 스킴을 태워봤지만,
  실기기와 같은 크로미움 엔진에서 "사용자 제스처 없이는 커스텀 스킴을 열 수 없다"는
  브라우저 보안 정책에 걸려 대부분 조용히 막히고, 심하면 페이지 로드 자체가 멈추는
  경우까지 있었다(Playwright 로 재현). 그래서 탭이 반드시 필요하고, 배너를 없애거나
  자동 리다이렉트로 바꾸면 안 된다.
- **폴링 중 자판 닫힘 방지** (`isTyping()` in `app.js`): 세 화면 다 `setInterval` 로
  주기적 재렌더(폴링)를 돈다. 각오·응원·점수 입력칸에 포커스가 있는 동안 재렌더가
  돌면 `innerHTML` 전체 교체로 그 입력칸 DOM 노드가 통째로 새 노드로 바뀌어 포커스가
  날아가고, 모바일에서는 그 순간 가상 키보드가 닫힌다. `document.activeElement` 가
  input/textarea 인 동안은 폴링 콜백이 그 턴을 건너뛰게 해서 막는다.

## 투표 1위 팀 특전 (비전력 보상)

코치들도 팀 선택 어필로 표를 받는 입장이라, 실력과 무관한 보상 두 가지를 준다.
스네이크 드래프트 자체는 건드리지 않았다 — 현행 방식이 이미 투표 1위 팀에게
1라운드 1픽을 준다.

1. **1라운드 1픽** — 코드 변경 없음. 현행 스네이크의 기본 동작.
2. **매치 순서권** — 매치데이 세 경기를 어떤 순서로 뛸지 투표 1위 팀 코치가 정한다.
   `draft_matches.match_order` + `coach_set_match_order` RPC. 코치 화면의
   "매치 순서권" 카드는 `ST.draft_order[0]` 이 자기 팀이거나 마스터일 때만 보인다.
   화살표로 순서를 바꾸고 저장하면 선수 `#경기` 채널의 경기 목록이 그 순서로 나간다.
   `master_reset` 이 `match_order` 를 `id` 순으로 되돌린다.

**"1위" 라는 표현 자체는 새로 감출 정보가 아니다.** REVEAL 단계가 이미 팀별
득표 순위(🥇🥈🥉, "드래프트 N번")를 공개하므로, `#우리팀`·순위표에 붙인
"🥇 투표 1위" 배지(`.pride-badge`)는 같은 정보를 다른 화면에서 다시 보여줄 뿐이다.
가려야 하는 건 **개인 지명 순번**(요구사항 1)이지 팀의 득표 순위가 아니다.

## 모의드래프트

실제 드래프트는 코치만 보는 비공개 진행이라, 선수들은 진행 픽 "수"와 팀 차례 말고는
볼 게 없어 심심하다. `#모의드래프트` 채널은 **코치들이 지금 쓰고 있는 드래프트
보드와 같은 화면**(선수 풀 · 팀 차례 배너 · 스네이크 트랙 · 팀 현황 · 픽 로그)으로,
선수도 실제와 **같은 지명 순서**(`draft_order`)를 따라 한 명씩 23명 전원을 대신
뽑아본다. 코치 6명이 아니라 혼자 3팀을 전부 대신 뽑는 1인 시뮬레이션이라 "내 차례"
개념은 없다 — 매 순번마다 화면이 "지금 차례" 팀을 알려주고, 탭한 선수를 그 팀에
배정한 뒤 다음 순번으로 넘어간다. 결과 공개 후 실제 명단과 몇 명이나 겹쳤는지로
순위를 매긴다.

**코치 픽과 완전히 대칭 구조로, 한 명 뽑을 때마다 바로 DB에 저장된다.** 처음엔
23명을 다 채운 뒤에야 한 번에 저장했는데, 그러면 중간에 새로고침하거나 다른
기기로 들어오면 진행 상황이 통째로 날아간다 — 코치 화면(`draft_picks`)은 픽
하나하나가 실시간으로 남는데 선수 쪽만 그렇지 않은 게 비대칭이라 바꿨다.
`draft_mock_picks(participant, seq, team, player_name)` 테이블이 `draft_picks`와
같은 모양이고, `mock_pick`/`mock_undo`/`mock_reset` 이 `coach_pick`/`coach_undo`와
같은 역할을 한다 — 차이는 `participant`(모의드래프트를 하는 사람)별로 완전히
독립된 자기만의 시퀀스라는 것뿐이다(코치 픽처럼 `draft_config` 행을 잠글 필요가
없다 — 서로 다른 참가자의 픽은 절대 경합하지 않는다).

- **REVEAL 발표가 다 끝나기 전엔 순서를 보여주지 않는다.** `draft_order` 는
  마스터가 "순서 확정"을 누르는 즉시 DB에 저장되지만(`master_lock_order`),
  `#결과발표` 화면은 3순위→2순위→1순위 순으로 한 칸씩 극적으로 공개한다
  (`reveal_step`). 모의드래프트가 이 값을 무시하고 바로 순서를 그려버리면
  코치 화면보다 먼저, `#결과발표`의 연출보다도 먼저 순서가 새는 셈이라
  `pi > 2 || reveal_step >= 3` 일 때만 연다 — DRAFT 단계에 들어가면 어차피
  전원에게 이미 공개된 정보이므로 무조건 연다. 이 게이팅은 클라이언트에서만
  하고 `mock_pick` 자체는 `coach_pick`처럼 phase만 본다 — `reveal_step` 까지
  서버에서 막지는 않는다(순서가 이미 DB에 있는 이상 진짜 방어선이 아니라
  연출 타이밍 문제라서, 코치 게이팅과 달리 신뢰 기반으로 둔다).
- **순번이 아니라 팀 배정만 화면에 보여준다.** 픽 하나하나가 실제로 `seq` 를
  달고 저장되긴 하지만, 이건 "몇 번째로 뽑았는지"를 실제 코치 픽처럼 채점에
  참고하려는 게 아니라 스네이크 순서(`draft_snake_team`)를 계산하기 위한
  내부용일 뿐이다. 화면에는 늘 "지금 차례"와 누적된 팀 현황만 보여주지,
  개인 지명 순번이라는 개념 자체가 이 기능엔 없다(내가 고른 순서일 뿐, 실제
  드래프트의 누군가의 순번이 아니다) — 요구사항 1과 무관하다.
  채점도 `draft_rosters.members`(이미 가나다순으로 공개되는 최종 명단)와만
  대조한다. 이 랭킹은 **참가자(맞힌 사람)** 를 줄 세우는 것이지 선수를
  평가하는 게 아니라서 요구사항 2(개인 평점·랭킹 금지)와도 무관하다 —
  PREDICT 단계의 우승 예측 적중자 랭킹과 같은 종류다.
- `draft_mock_picks` 는 SELECT 정책이 열려 있다(누구나 서로의 진행을 볼 수
  있음, 명단 공개가 의도인 테이블들과 동일 패턴). 쓰기는 `mock_pick` 으로만
  가능하고, 이미 뽑은 선수 재지명·23명 초과·`REVEAL`/`DRAFT` 밖에서의 시도를
  전부 거부한다. `mock_reset` 은 그 참가자의 픽을 통째로 지워 처음부터 다시
  하게 해준다(투표처럼 한 번뿐인 게 아니라 각오/캐릭터처럼 계속 고칠 수 있는
  쪽을 택했다 — 드래프트 창이 며칠씩 열려 있어서, 생각이 바뀌면 바로 반영되는
  게 더 재밌다).
- `mock_draft_leaderboard()` — `draft_rosters` 를 펼쳐 만든 (팀, 선수) 답안표와
  각 참가자의 픽을 대조해 정답 수를 센다. **23명을 다 채운 참가자만** 순위에
  들어간다(부분 진행은 채점 공정성이 깨지므로 제외). `SECURITY DEFINER` 가
  아닌 일반 `stable sql` 함수다(`vote_tally`/`standings` 와 같은 스타일) —
  쓰기가 없고 이미 공개된 테이블만 읽어서 권한 상승이 필요 없다.
  `draft_rosters` 가 비어 있으면(RESULT 공개 전) 완주자 전원 0점으로 나올
  뿐이라 별도 예외 처리를 하지 않는다.
- 참여 신원은 무기명이 아니라 **본인이 고른 실명**이다(`draft_players.name`
  그대로, `draft_mock_picks.participant`) — 투표와 달리 팀 배정을 맞히는 건
  민감 정보가 아니다. 이름을 아직 안 고른 사람이 참여하면(`#내캐릭터` 와
  같은 패턴) 이름 선택 그리드를 먼저 보여주고, **첫 픽이 성공한 시점에만**
  `store.set('name', ...)` 으로 전역 신원을 확정한다 — 이름만 고르고 아직
  한 명도 못 뽑았다면 신원이 안 묶인다.

## 단계가 바뀌면 그 채널로 자동 이동

`index.html`(`load()`)은 폴링마다 이전에 본 `phase`/`reveal_step` 을 기억해뒀다가,
바뀐 게 감지되면 `S.active` 를 그 단계에 맞는 채널로 강제로 옮긴다 — 다른 채널을
보고 있다가 마감·발표·결과공개가 떠도 못 보고 지나치는 일이 없게 하려는 것이다.
처음 들어왔을 때(`S.active` 없음)만 기본 채널로 보내던 걸, **세션이 열려 있는
동안 일어나는 전환에도** 적용한 것이 핵심이다. 단계 전환(`phaseChanged`)이 우선이고,
`REVEAL` 안에서 발표가 다 끝나(`reveal_step` 0→3) 모의드래프트가 막 열리는 순간
(`mockJustUnlocked`)은 그다음 우선순위로 `#모의드래프트` 로 보낸다 — 같은 틱에
단계 전환과 모의드래프트 개방이 동시에 잡히면(마스터가 순서 확정 후 발표 3단계를
한 번에 몰아 누른 경우) 발표 결과를 보여주는 쪽이 우선이다. 새 채널을 추가하거나
단계를 늘릴 땐 `defaultChannel()` 의 매핑과 이 감지 로직을 같이 봐야 한다.

활성 채널만 바뀌고 **메뉴(채널 목록) 순서는 원래 고정**이었는데, 모바일 상단 탭
(`.topbar-channels`)은 가로 스크롤 스트립이라 순서상 뒤쪽인 채널은 화면 밖으로
밀려 존재 자체를 못 보는 문제가 있었다 — 드래프트 단계에서 `#드래프트`·
`#모의드래프트`가 4·5번째라 스크롤해야만 보였다. `PHASE_CHANNELS`(단계별 "지금
가장 중요한 채널" 목록, `defaultChannel()`도 여기서 첫 번째 값을 가져다 쓴다)를
따로 두고, `menuChannels()`가 그 목록을 메뉴 맨 앞으로 끌어온 뒤 나머지를
원래 순서로 붙여 사이드바·모바일 탭에 그린다. 그래서 스크롤 없이 항상 왼쪽
끝(모바일 탭 시작 지점)에서 지금 단계의 채널을 볼 수 있다 — 드래프트 단계엔
`#드래프트`·`#모의드래프트`가 나란히 맨 앞, 결과 공개 단계엔 `#우리팀`·`#응원`이
맨 앞으로 온다. `defaultChannel()`과 값이 어긋나면 안 되므로 새 채널을 추가할 땐
`PHASE_CHANNELS` 하나만 고치면 된다(따로 두 곳을 맞출 필요 없음).

## 절대 깨면 안 되는 것

이 프로젝트의 존재 이유에 가까운 제약이라, 디자인 개편 중에도 유지해야 한다.

1. **개인 지명 순번은 어디에도 노출되지 않는다.** 공개 명단은 항상 가나다순.
2. **선수 개인 평점·랭킹을 만들지 않는다.** 집계는 팀 단위만.
3. **투표는 무기명.** `draft_vote_marks`(누가 했나)와 `draft_ballots`(뭘 골랐나)는
   연결 키가 없고, ballot에는 시각을 저장하지 않는다.
4. **드래프트 진행 상황은 코치만 본다.** 선수에게 나가는 건 진행 픽 "수"와 팀 차례뿐.
5. UI 문구에 "1순위 지명", "마지막 픽" 같은 서열 표현을 쓰지 않는다.

## 백엔드 (Supabase)

`draft_` 접두사 테이블. 프로젝트 `qjvuxwldlviknhiydoxw`.

- `draft_pledges` · `draft_looks` · `draft_coach_cards` 는 **이름 공개가 의도**라
  SELECT 정책만 열려 있다. 쓰기는 `submit_pledge` / `submit_look` / `coach_set_card` 를
  통해서만 가능하고, 팔레트 범위 밖의 값·모르는 키·숫자가 아닌 값은 서버에서 전부 버린다.
- **코치도 표를 받는다.** `draft_coach_cards`(coach_id·이름·팀·룩·각오)를 선수 투표 화면의
  팀 선택 아래에 노출한다. 암호가 든 `draft_coaches` 는 정책 없음(anon 차단)을 유지하고,
  카드 테이블에는 암호를 넣지 않는다. `coach_set_card` 는 **암호에서 코치 신원을 끌어내므로**
  남의 카드는 건드릴 수 없다 (코치 이름을 인자로 받지 않는다).
- `draft_picks` · `draft_wishes` · `draft_coaches` · `draft_ballots` 는
  **RLS 정책이 아예 없다** = anon 접근 전면 차단. 이게 요구사항 4·5의 방어선이므로
  편의를 위해 정책을 추가하지 말 것.
- `draft_players.attending` — 불참 3명을 표시하는 컬럼이지만 **지명을 막지 않는다.**
  투표·각오·캐릭터(`submit_vote`/`submit_pledge`/`submit_look`)는 애초에 이 컬럼을
  보지 않고, `coach_pick`도 전체 인원(`count(*) from draft_players`, 현재 23)을
  기준으로 픽 수를 셀 뿐 대상(`p_player`)엔 이 컬럼으로 어떤 제한도 걸지 않는다 —
  애초에 코드가 "참석자 먼저" 순서를 강제한 적이 없다. 초반엔 참석자를 다 뽑은 뒤
  불참자를 이어서 뽑는 관행으로 흘러갔지만, 한 팀이 불참자를 먼저 뽑고 싶다고 해서
  그 관행도 접었다 — 지금 차례인 팀이 스네이크 순번만 지키면 참석 여부와 무관하게
  아무 선수나 뽑을 수 있다. 유일한 쓰임은 `coach_state().absentees` 로 코치 화면
  선수 풀에 "🚫 불참" 라벨을 붙여 누가 못 오는지 참고하게 돕는 것뿐이다. `assets/config.js`의
  `TOTAL_PICKS`(=전체 인원수)도 여기서 나온다 — 팀 수로 안 나눠떨어지면 스네이크
  마지막 라운드가 부분적으로만 찬다(`snakeTrackHtml`가 넘치는 칸을 스스로 잘라낸다).
  ⚠️ **인원수(`TOTAL_PICKS`)를 바꾸면 `draft_picks_seq_check` 체크 제약도 같이
  바꿔야 한다** — `check (seq >= 0 and seq <= TOTAL_PICKS - 1)` 형태로 DB에 걸려
  있는데, 18명이던 예전 시즌 값(`seq <= 17`)이 23명으로 늘어난 뒤에도 안 바뀐 채
  남아있었다. 그 결과 19번째 픽(`seq = 18`)부터 전부 `error=23514`(체크 제약
  위반)로 조용히 막혀서, 실전 드래프트가 18/23에서 한 시간 넘게 멈추는 사고가
  실제로 있었다(2026-09-12, `apply_migration`으로 `seq <= 22`로 즉시 수정).
  클라이언트 에러 메시지 매핑(`ERROR_MESSAGE`)엔 없는 코드라 "문제가 생겼어요"
  라는 의미 없는 문구만 뜨고 원인을 알 수 없었다 — 이런 종류의 실패는
  Supabase 로그(`edge_logs`)의 `response.headers.proxy_status`에서
  `PostgREST; error=<SQLSTATE>` 형태로 진짜 원인이 남으므로, 다음에 비슷한
  "이유 없이 저장이 안 돼요" 신고가 오면 여기부터 확인할 것.
  ⚠️ **WHERE 없는 `delete from table;` 을 함수 안에 쓰지 말 것.** 같은 날
  `master_publish`의 `delete from draft_rosters;`(WHERE 없음)가 마스터의
  "결과 공개하기"를 계속 막은 두 번째 사고가 있었다 — Supabase 로그
  (`postgres_logs`)에 `"DELETE requires a WHERE clause"`가 정확히 그 요청
  타이밍에 찍혀 있었다(직접 SQL로 재현은 안 됐다 — MCP 실행 경로는 이 제약을
  우회하는 듯하다. `master_reset`에도 같은 패턴의 무조건 `delete from` 이
  10군데 더 있어서 같이 손봤다). 조치는 전부 `delete from table where true;`
  로 바꾸는 것 — 지우는 대상은 완전히 같고 문법만 WHERE 절을 갖춘다. 앞으로
  전체 삭제가 필요한 함수를 새로 짤 때도 `where true`를 붙일 것.
- 모든 쓰기는 `SECURITY DEFINER` 함수 경유. 코치 기능은 8자리 암호로 게이트.
  `coach_state / coach_pick / coach_undo / coach_wish`,
  `master_set_phase / master_set_reveal_step / master_lock_order / master_publish /
   master_set_score / master_hide_comment / master_reset`,
  `coach_set_card`, `coach_set_match_order`,
  `submit_vote / submit_pledge / submit_look / submit_prediction / submit_comment /
   vote_tally / standings`
- 픽 확정은 `draft_config` 행을 잠그는 원자적 트랜잭션 (동시 픽 유실 방지).
- 코치 암호는 저장소에 두지 않는다. DB에만 있고 코치에게 전달한다 (개별 DM이든
  코치 6명만 있는 단톡방이든 무방 — 선수 단톡방에만 절대 올리지 않는다).
  코치들끼리 아는 사이라 "초성 2글자 + 숫자 2자리"(예: `wb52`)로 간단하게 잡아뒀다.
  ⚠️ `draft_coach_cards.coach_id` 는 SELECT 정책이 열려 있어 **누구나(선수 포함) 읽을
  수 있다** — 그래서 암호를 `id` 값 그대로(`wb` 등) 쓰면 그 자체로 새는 것과 같다.
  반드시 `id` 와 다른 문자열로 잡을 것. 마스터 암호는 이 간소화 대상이 아니다 —
  단계 전환·전체 초기화 권한이라 여전히 복잡한 값을 유지하고 코치방에도 공유하지 않는다.

## 지금 열려 있는 작업

**스포츠 감성으로 전면 디자인 개선.** 현재는 슬랙 구조 + 파스텔 라운드라
"스포츠"보다 "귀여운 커뮤니티" 쪽에 가깝다. 손볼 만한 지점:

- 헤더/히어로가 밋밋함 — 매치데이 카운트다운, 팀 컬러 대비를 살린 스코어보드 느낌
- 순위표·승률 바가 일반 표에 가까움 — 스포츠 중계 그래픽 톤으로
- 발표(REVEAL) 연출이 정적 — 순위 공개 시 임팩트 부족
- 팀 컬러가 카드 테두리 정도로만 쓰임 — 팀 아이덴티티를 더 강하게
- 모바일(카톡 인앱 브라우저)이 주 사용 환경이라 여기서 먼저 확인할 것

## 배포

GitHub Pages (`main` 브랜치 root). 푸시하면 1~2분 뒤 반영.

⚠️ **asset 링크에는 반드시 `?v=<해시>` 가 붙어 있어야 한다.** `tools/stamp-assets.sh`
가 CSS·JS 내용의 sha1 앞 8자리를 세 HTML 에 박아준다. Stop 훅이 커밋 직전에
자동으로 돌리므로 평소엔 신경 쓸 필요 없지만, 손으로 배포할 땐 직접 돌릴 것.
안 하면 카톡 인앱 브라우저가 옛 CSS 를 물고 있어 **HTML 만 새 버전인 상태**가 되고,
레이아웃이 통째로 깨져 보인다 (실제로 겪었다).
드래프트 진행 중에는 `main`에 바로 푸시하지 말고 확인 후 올릴 것 —
선수 23명이 실시간으로 보고 있는 화면이다.
