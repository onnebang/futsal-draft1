# FC풉살 2026 하반기 내부매치 드래프트 시스템 — 개발 계획서

> 작성일 2026-09-08 (화) · 드래프트: 9/11(금) ~ 9/13(일) 18:00, 코치 비동기 진행 · 내부매치: 12/19(토)
> 대상 저장소: `onnebang/futsal-draft1`

> **📌 진행 상태 (2026-09-08 업데이트)**
> 상반기는 코치만 보는 화면이라 기능 위주로 대충 만들었지만, 이번엔 **선수 18명 전원에게 공개되는 화면**이라 비주얼을 화려하게 갈 예정.
> → **디자인 참고자료를 코치님이 전달할 예정이라, 실제 UI 비주얼 작업은 그 자료를 받은 뒤 진행한다.** 이 문서는 그 전까지 구조·데이터모델·보안 로직·일정을 먼저 확정해 두는 버전.
> 백엔드는 Firebase에 얽매이지 않아도 되고, 코치 로그인도 "인원 고정된 1회성 이벤트"라는 특성상 하드코딩 기반으로 충분하다는 방향 확인 — §5.1a, §7a 참고.

---

## 1. 개요

### 1.1 목표
상반기 드래프트 프로그램을 발전시켜, **선수 참여형 → 코치 드래프트 → 공개 발표 → 3개월 하이프 빌드업**까지
하나의 사이트에서 굴러가는 시스템을 만든다.

상반기와 결정적으로 다른 점 5가지:
1. **선수도 참여한다.** 선호 팀 무기명 투표로 드래프트 지명 순서가 결정된다.
2. **드래프트는 완전 비공개다.** 선수는 진행 화면 자체를 볼 수 없다 (기술적으로 차단).
3. **드래프트가 끝나도 하이프가 이어진다.** 예측 · 승률 · 댓글이 9월~12월 3개월을 채운다.
4. **선수는 자기 얼굴을 갖는다.** 캐릭터를 14가지(자세·키·머리 길이/스타일/색·피부색·표정·상의 무늬/색·하의 무늬/색·양말 무늬/색·신발색)로 직접 꾸미고, 그 모습이 코치 드래프트 보드와 팀 명단에 그대로 나온다.
5. **선수도 자신을 어필한다.** 드래프트 전 이름과 함께 "각오 한마디"를 남기면, 코치는 드래프트 중 참고하고 선수는 대기 화면에서 서로의 각오를 구경한다 (§8.1, §8.3).

### 1.5 드래프트 진행 방식 — 하루 라이브 이벤트가 아니라 3일 비동기 진행
코치 6명이 한자리에 동시에 모이기 어렵다는 현실을 반영해, 드래프트는 **9/11(금) ~ 9/13(일) 18:00**의 3일짜리 창(window)으로 열어두고, 각 코치가 자기 팀 차례가 되면 틈틈이 접속해 픽하는 방식으로 진행한다. 한 팀이 픽을 마치면 **카카오톡으로 다음 팀에게 직접 알린다** ("저희 A팀 뽑기 끝났어요, B팀 코치님 부탁드려요 🙏") — 사이트가 자동으로 알림을 보내지는 않는다 (§8.3, §11.2).

### 1.2 참가 규모
| 항목 | 값 |
|---|---|
| 선수 | 18명 |
| 팀 | 3팀 × 6명 |
| 코치 | 6명 (팀당 2명) |
| 경기 | 5 vs 5, 12분 매치 |
| 드래프트 | 스네이크 6라운드 × 3픽 = **총 18픽** |

### 1.3 팀 구성
| 코드 | 팀명 | 코치 | 컬러(안) |
|---|---|---|---|
| `A` | 민호경표팀 | 민호, 경표 | `#3182ce` (블루) |
| `B` | 원배원언팀 | 원배, 원언 | `#e53e3e` (레드) |
| `C` | 경목준하팀 | 경목, 준하 | `#38a169` (그린) |

> ⚠️ 상반기와 코치 조합이 다릅니다. 상반기 `index.html` 기준 (민호+준하 / 원배+경표 / 경목+원언) → **하반기 재편**. 하드코딩된 `coachAuth` 팀 매핑을 반드시 갱신할 것.

### 1.4 선수 명단 (18명)
```
가은 · 나영 · 다이 · 민지 · 서윤 · 선민 · 수연 · 은재 · 이지
지혜 · 해수 · 혜린 · 혜선 · 혜은 · 혜진 · 화인 · 효린 · 호원
```
> 상반기에 있던 `specialPlayers`(송희·재인·이지 자동 배정) 개념은 **폐지**한다. 18명 = 3팀 × 6명으로 딱 떨어지므로 특수 배정이 필요 없다. (이지는 이번엔 일반 드래프트 대상)

---

## 2. 상반기 시스템 리뷰 — 계승할 것 / 고칠 것

현재 `index.html`(33KB, 단일 파일)을 분석한 결과.

### ✅ 계승할 것
- **단일 HTML + Firebase RTDB + GitHub Pages** — 서버 비용 0원, 카톡 링크 하나로 접속. 이번에도 유지.
- **코치별 "찜(👀) / 원픽(🔥)" 기능** — 코치들 사이 눈치싸움이 재밌었던 핵심. 유지.
- **완료 시 발표 모드(`.final-view`) + confetti** — 연출 좋음. 확장해서 재사용.
- **Pretendard 폰트, 모바일 우선 레이아웃** — 그대로.

### ❌ 반드시 고칠 것

| # | 상반기 문제 | 영향 | 이번 대응 |
|---|---|---|---|
| 1 | **DB가 사실상 전면 공개** — RTDB 규칙 없이 클라이언트에서 `draftState` 통째로 읽음 | 선수가 개발자도구/콘솔로 지명 순번을 그대로 볼 수 있음. **요구사항 4·5가 원천 무효** | Firebase **보안 규칙 + 코치 계정 인증**으로 `/draft` 경로 차단 (§7) |
| 2 | **`db.ref('draftState').set(state)`** — 전체 상태를 통째로 덮어씀 | 두 코치가 거의 동시에 픽하면 **한 픽이 통째로 유실**됨 (Last-write-wins) | `runTransaction()` 원자적 갱신 (§9.3) |
| 3 | **되돌리기(Undo) 없음** | 오지명 시 마스터 리셋 = 처음부터 다시 | 픽 취소 1회 + 관리자 강제 롤백 (§9.4) |
| 4 | `state.drafted` 배열 = **지명 순서 그대로** DB에 저장 | 공개 명단은 `.sort()` 했지만 원본 순서가 DB에 남아있음 | 공개용 `/public/rosters`는 **순서 정보를 제거한 별도 노드**에 1회만 기록 (§9.5) |
| 5 | 선수 참여 기능 전무 (관전만 가능) | — | 투표·예측·댓글 신설 |
| 6 | `mock_draft.html`이 실 DB(`draftState`)를 공유 | 테스트가 실 데이터를 오염 | 테스트는 `/h2test` 별도 네임스페이스 or `?demo=1` 로컬 모드 |

---

## 3. 핵심 원칙 — "감정 안전 6원칙"

> 이 프로젝트의 최우선 제약. 기능보다 우선한다.
> 재미를 위해 이 중 하나라도 어기는 기능은 **넣지 않는다.**

1. **개인 지명 순번은 어디에도 공개되지 않는다.** 화면·URL·DB 공개 노드 어디서도 복원 불가능해야 한다.
2. **개인 평점/등급/랭킹을 만들지 않는다.** 상반기의 "Hype 랭킹(선수별 점수 Top 10)"은 **이번에 삭제**한다. 선호도·전력지수는 **팀 단위 집계값만** 다룬다.
3. **공개 명단에서 순서 정보를 제거한다.** 가나다순 고정 노출 (§9.5).
4. **투표는 무기명.** "누가 투표했는지"와 "무엇을 투표했는지"를 **DB상 연결할 수 없게** 물리적으로 분리 저장한다 (§6.2).
5. **댓글은 닉네임 기반 + 코치 삭제권.** 상단에 선플 가이드 고정.
6. **UI 문구에서 서열 표현 금지.** "1순위 지명", "마지막 픽", "최하위" 같은 단어를 화면에 쓰지 않는다. 대신 "확정 명단", "우리 팀".

---

## 4. 전체 플로우 (상태 머신)

사이트 전체가 `/config/phase` 값 하나로 움직인다. 관리자가 단계를 넘긴다.

```
 ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
 │  VOTE    │ → │  REVEAL  │ → │  DRAFT   │ → │  RESULT  │ → │ PREDICT  │ → │  MATCH   │
 └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
   선호팀 투표     프듀101식        코치 전용        팀 명단          순위 예측         경기 결과
   (무기명)        결과 공개       비공개 진행      가나다순 공개    + 승률 + 댓글      + 적중자 발표
      │              │               │               │               │               │
  요구사항 1      요구사항 2      요구사항 3·4      요구사항 5      요구사항 6·7·8     (확장)
```

| Phase | 선수 화면 (`index.html`) | 코치 화면 (`coach.html`) | 기간 |
|---|---|---|---|
| `VOTE` | 선호팀 투표 폼 + 참여 현황(n/18) + 각오 한마디 남기기 | 투표 현황 모니터링 | 9/9(수) 10:00 ~ 9/10(목) 22:00 |
| `REVEAL` | 프듀101 발표 화면 (실시간 동기화) | 발표 단계 컨트롤러 | 9/11(금) 오전 |
| `DRAFT` | "🔒 드래프트 진행 중" 대기 화면 — 진행률(n/18) + 각오 한마디 게시판 | **드래프트 보드** (각오 한마디 말풍선 포함) | 9/11(금) ~ 9/13(일) 18:00, 코치 비동기 진행 |
| `RESULT` | 3팀 확정 명단 공개 + confetti | 결과 확인 | 9/13(일) 저녁 |
| `PREDICT` | 순위 예측 + 승률 + 팀별 댓글 | 댓글 관리 | 9/13(일) 밤 ~ 12/18 |
| `MATCH` | 실시간 순위표 + 예측 적중자 | 경기 결과 입력 | 12/19(토) |

---

## 5. 시스템 아키텍처

### 5.1 기술 스택 (상반기 계승 + 보안 강화)
| 레이어 | 선택 | 이유 |
|---|---|---|
| 호스팅 | **GitHub Pages** (`onnebang/futsal-draft1`) | 무료, 카톡 링크 즉시 배포, 이미 저장소 있음 |
| 프론트 | **Vanilla JS + 단일 HTML/페이지 (화면별 분리)** | 빌드 파이프라인 없음 = 유지보수 최소. 화려한 비주얼은 CSS/JS 애니메이션으로 — 디자인 참고자료 수신 후 §8을 갈아엎을 예정 |
| 백엔드/실시간 DB | **§5.1a에서 결정** (Firebase 또는 Supabase) | 아래 참고 |
| 연출 | `canvas-confetti` (기존) + CSS 애니메이션, 필요 시 추가 연출 라이브러리 | 디자인 확정 후 보강 |
| 폰트 | Pretendard (기존, 화려한 방향에 맞춰 서브 폰트 추가 검토) | 동일 |

### 5.1a 백엔드 플랫폼 — Firebase 꼭 아니어도 됨

상반기는 Firebase RTDB를 그대로 재사용했지만, 이번엔 어디든 편한 곳으로 가도 무방하다는 코치님 의견에 따라 두 옵션을 비교한다.

| | **Firebase RTDB (계승안)** | **Supabase (신규 후보)** |
|---|---|---|
| 데이터 모델 | JSON 트리 | Postgres 테이블 (관계형) |
| 실시간 동기화 | `on('value')` — 상반기에 이미 검증됨 | Realtime 채널 (Postgres 변경분 구독), 동급 |
| 보안 | Realtime Database Rules (JSON, §7 설계 완료) | Row Level Security (SQL 정책) — 표현력은 더 좋음 |
| 인증 | Firebase Auth | Supabase Auth (이메일/매직링크/OTP 내장) |
| 이번 세션에서 내가 직접 구축 가능한가 | ❌ — 콘솔에서 사람이 직접: Auth 활성화, 계정 7개 생성, UID 복사해서 DB에 입력 (§7 "적용 절차" 5단계, 전부 수작업) | ✅ — 이 세션에 **Supabase MCP 도구가 연결되어 있어** 테이블·RLS 정책·시드 데이터까지 내가 직접 실행 가능. 사람이 할 일은 프로젝트 생성 승인 정도 |
| 상반기 자산 재사용 | DB 구조·규칙 설계(§6·§7) 그대로 이식 | 같은 익명성 원칙(투표 mark/ballot 분리, 코치 전용 노드)을 SQL 테이블+RLS로 1:1 이식 필요 |

**추천: Supabase.** 기능·비용 차이보다 결정적인 이유는 하나 — **이 세션에서 제가 도구로 직접 테이블/보안정책/시드데이터까지 구축·검증할 수 있어서, 코치님이 콘솔에서 수작업하실 일이 거의 없습니다.** Firebase는 상반기 프로젝트가 이미 있다는 장점은 있지만, Auth 계정 생성과 UID 매핑을 사람이 직접 해야 합니다(§7의 "적용 절차").

> 최종 선택은 디자인 참고자료 받을 때 같이 확정하면 됩니다. §6(데이터 모델)·§7(보안 규칙)은 Firebase 기준으로 이미 설계돼 있고, Supabase로 갈 경우 **같은 원칙(마크/발롯 분리, 코치 전용 read 차단, 트랜잭션 픽 확정)을 SQL 테이블 + RLS 정책으로 동일하게 재현**하면 되므로 설계 자체가 버려지진 않습니다.

### 5.1b 코치 로그인 — 하드코딩 유지 + 살짝 보강

상반기 방식(2글자 암호 `mh`, `wb`, `kp`...)이 "1회성·고정 인원 이벤트"에는 충분하다는 데 동의. 다만 그대로 쓰기엔 두 가지 약점이 있어 아래처럼 보강 제안:

1. **암호가 너무 짧고 유추하기 쉬움** — 이니셜 2글자라 어느 정도 아는 사람이면 추측 가능. 선수에게 알려질 위험보다, 화면이 이번엔 선수에게도 완전 공개되므로 **드래프트 화면 URL/암호가 어쩌다 노출됐을 때의 파급력이 상반기보다 큼.**
2. **암호가 코드에 그대로 박혀있어 소스만 보면 즉시 유출** — GitHub Pages는 소스가 100% 공개이므로 `coachAuth` 객체를 그대로 두면 개발자도구 없이 "파일 보기"만 해도 다 보임.

**제안 (하드코딩 기조는 유지, 노출 지점만 분리):**
- 코치 6명 + 마스터 1명에게 **각자 다른 8자리 랜덤 문자열**을 부여 (예: `a3f9k2mq`) — 여전히 "하드코딩된 고정 암호" 방식 그대로, 사람이 외우기 조금 불편해지는 대신 추측 불가.
- 이 암호는 **소스 코드(`config.js`)에 평문으로 넣지 않고**, DB(Firebase면 `coachSecrets` 노드에 `.read: false`로 차단 / Supabase면 RLS로 select 차단된 테이블)에만 저장. 클라이언트는 입력받은 암호를 그 노드에 "이 값과 일치하는 게 있으면 나에게 코치 권한을 달라"는 식으로만 검증 요청 → 소스를 열어봐도 암호 목록이 안 보임.
- 전달은 **카톡 단체방이 아니라 각 코치에게 개별 DM**으로. (상반기도 아마 이렇게 하셨겠지만 한번 더 체크포인트로 남겨둠)
- 이 정도면 "로그인 화면 하나 만들고 계정 관리하는" 번거로움 없이, 상반기 방식의 편함을 유지하면서 노출 위험만 줄일 수 있습니다.

> 더 탄탄하게 가고 싶다면 Supabase Auth 매직링크(코치 이메일로 로그인 링크 발송)도 옵션으로 열어두겠지만, 6명 규모에 "1회성"이라는 코치님 판단에 따라 **기본은 위 하드코딩 보강안으로 진행**.

### 5.1c 디자인 — 슬랙(Slack) 스타일 ✅ 확정
참고자료를 따로 찾지 못해, **슬랙 워크스페이스 메타포**로 확정했다. 전원이 카톡으로 들어오는 웹앱이라 "채팅앱처럼 익숙한" 인상이 진입 장벽을 낮춘다.

- **컬러**: 오버진 퍼플(`#4A154B`) 헤더·사이드바 + 슬랙 그린(`#007A5A`) CTA
- **팀 컬러 = 슬랙 브랜드 4색**: 민호경표팀 `#36C5F0`(블루) · 원배원언팀 `#E01E5A`(레드) · 경목준하팀 `#2EB67D`(그린), 강조는 `#ECB22E`(옐로)
- **구조**: 각 단계가 하나의 **채널**(`#투표` `#결과발표` `#드래프트` `#우리팀` `#예측` `#응원`). 아직 안 열린 채널은 🔒로 표시되고, 현재 단계 채널에는 알림 점이 붙는다
- **각오 한마디·응원 댓글**은 실제 슬랙 메시지 형태(정사각 아바타 + 굵은 이름 + 시각)로 렌더링
- 모바일에서는 사이드바가 상단 가로 채널 바로 바뀐다 (카톡 인앱 브라우저 우선)
- 폰트는 Pretendard (슬랙의 Lato 계열 인상 + 한글 가독성)

### 5.2 파일 구조 (제안)
```
/
├─ index.html          # 선수용 허브 — phase에 따라 투표/발표/결과/예측/댓글 자동 전환
├─ coach.html          # 🔒 코치 전용 — Firebase Auth 로그인 + 드래프트 보드
├─ admin.html          # 🔒 마스터 전용 — phase 전환, 순서 확정, 댓글 관리, 리셋
├─ assets/
│  ├─ app.js           # 공통 유틸 (firebase init, phase 구독, 셔플, 포맷)
│  ├─ config.js        # 팀·선수·코치 명단 (한 곳에서만 수정)
│  └─ style.css        # 공통 스타일
├─ database.rules.json # ⭐ Firebase 보안 규칙 (버전 관리 대상)
├─ docs/
│  ├─ OPERATIONS.md    # D-day 운영 매뉴얼 + 카톡 공지 문구
│  └─ QA.md            # 리허설 체크리스트
├─ archive/2026-h1/    # 상반기 index.html, mock_draft.html 보존
└─ PLAN.md             # (이 문서)
```

> **`assets/config.js` 분리가 중요합니다.** 상반기엔 선수/코치 명단이 `index.html`과 `mock_draft.html`에 중복 하드코딩되어 있어, 명단이 바뀌면 두 군데를 고쳐야 했습니다. 이번엔 단일 소스로.

---

## 6. 데이터 모델

루트를 `/h2`로 분리해 상반기 `/draftState` 데이터를 보존한다.

```jsonc
{
  "h2": {
    "config": {                       // 공개 읽기 / 마스터만 쓰기
      "phase": "VOTE",                // VOTE|REVEAL|DRAFT|RESULT|PREDICT|MATCH
      "revealStep": 0,                // 0=대기 1=3위공개 2=2위공개 3=1위공개
      "draftOrder": ["A", "C", "B"],  // 투표로 확정된 지명 순서
      "resultPublishedAt": 1765...    // RESULT 진입 시각
    },

    "coachUids": {                    // 아무도 못 읽음 (규칙 내부 참조 전용)
      "<firebase-uid>": { "name": "민호", "team": "A" }
    },

    "vote": {
      "marks":  { "<이름해시>": true },              // ← "누가 투표했나" (내용 없음)
      "ballots": { "<랜덤32자>": {                   // ← "무엇을 투표했나" (이름 없음)
          "first": "A", "second": "C", "nick": "풉살고양이"
      }}
    },

    "pledges": {                      // ⚠️ 무기명이 아님 — 선수가 자기 이름으로 남기는 자기소개 카드
      "<선수이름>": { "text": "뽑아주세요! 열심히 뛸게요 💪", "ts": 176... }  // 1인당 1개, 덮어쓰기로 수정 가능
    },

    "draft": {                        // 🔒 코치 인증 필수 — 선수 접근 불가
      "picks": [ { "seq": 0, "team": "A", "player": "해수", "by": "mh", "ts": 176... } ],
      "wishes": { "mh": { "super": "해수", "normal": ["가은", "혜린"] } },
      "cursor": 0,                    // 트랜잭션으로만 증가
      "status": "running"             // running|done
    },

    "public": {
      "progress": { "cursor": 6 },                                      // 드래프트 진행 픽 "수"만 공개 (누가·어느팀인지는 비공개)
      "rosters": { "A": ["가은","나영",...], "B": [...], "C": [...] },  // 가나다순, 완료 시 1회만 기록
      "power":   { "A": 0.34, "B": 0.33, "C": 0.33 },                   // 팀 단위 집계값만
      "winprob": { "A": 0.41, "B": 0.32, "C": 0.27 },
      "predict": {
        "marks":   { "<이름해시>": true },
        "ballots": { "<랜덤32자>": { "rank": ["A","B","C"], "nick": "..." } }
      },
      "comments": { "A": { "<pushId>": { "nick": "...", "text": "...", "ts": 176... } } }
    },

    "match": {
      "results": [ { "home": "A", "away": "B", "hs": 3, "as": 2 } ]
    }
  }
}
```

### 6.1 왜 `/draft`와 `/public/rosters`를 나누는가
`/draft/picks`에는 지명 순서가 그대로 남는다 (코치는 봐야 하므로). 이 노드는 **보안 규칙으로 선수가 읽을 수 없다.**
드래프트 완료 시점에 코치 앱이 순서를 제거·정렬한 결과만 `/public/rosters`에 **딱 한 번** 기록한다.
→ 선수가 접근 가능한 데이터만으로는 지명 순번을 **복원할 방법이 원리적으로 없다.** (요구사항 4·5 충족)

### 6.2 왜 `marks`와 `ballots`를 나누는가 (무기명 보장)
- `marks`: `{ 해시("해수"): true }` — 투표 여부만. 무엇을 골랐는지 없음.
- `ballots`: `{ 랜덤키: {first:"A"} }` — 선택만. 누구인지 없음.
- 두 레코드 사이에 **연결 키가 없다.** 관리자가 DB를 직접 열어봐도 매칭 불가능.

**단, 3가지 함정을 반드시 피해야 함:**
1. ❌ `push()` 사용 금지 → Firebase push 키는 **타임스탬프 인코딩**이라 시간순 정렬이 된다. `crypto.randomUUID()` 기반 랜덤 키를 쓸 것.
2. ❌ ballot에 `ts` 저장 금지 → 마크 기록 순서와 대조하면 역추적 가능.
3. ❌ 투표 진행 중 ballots 읽기 금지 → 규칙에서 `phase === 'VOTE'`인 동안 read를 막는다. (실시간 관찰 + 카톡 "나 방금 했어" 조합으로 특정되는 걸 방지)

### 6.3 `pledges`(각오 한마디)는 왜 무기명이 아닌가
`vote`/`predict`와 달리 `pledges`는 **의도적으로 이름을 공개**하는 자기소개 카드다 — "나를 뽑아달라"는 어필이 목적이므로 익명이면 기능 자체가 성립하지 않는다. 그래서 마크/발롯 분리를 적용하지 않고, `<선수이름>`을 키로 곧바로 저장한다. 대신:
- 팀 선호도(`vote`)와는 완전히 다른 노드라 두 데이터를 절대 섞지 않는다.
- 코치 드래프트 보드에서는 참고용 말풍선으로만 쓰고, 픽 결정에 강제 반영하지 않는다.
- 문구는 40자 이내로 제한하고, 실명 저격·타 선수 비방이 들어가지 않도록 제출 전 안내 문구를 노출한다.

### 6.4 `public/progress`는 왜 공개해도 안전한가
드래프트가 몇 픽까지 진행됐는지(`cursor` 숫자)는 **누가·어느 팀에 갔는지를 전혀 드러내지 않는다.** 팀의 지명 순서(`draftOrder`)는 이미 REVEAL 단계에서 전원에게 공개된 정보이므로, 진행 픽 수와 조합해도 "지금 어느 팀 차례인지" 이상은 알 수 없다 — 이는 원칙 1(개인 지명 순번 비공개)을 침해하지 않는다. 이 값 덕분에 선수 대기 화면(§8.3b)에서 "18명 중 6명 확정" 진행률을 실시간으로 보여줄 수 있다.

---

## 7. Firebase 보안 규칙 (핵심)

`database.rules.json` — **이 파일이 요구사항 4·5의 유일한 실질적 방어선이다.**

```json
{
  "rules": {
    "h2": {
      "coachUids": {
        ".read": false,
        ".write": false
      },

      "config": {
        ".read": true,
        ".write": "auth != null && root.child('h2/coachUids/'+auth.uid+'/master').val() === true"
      },

      "vote": {
        "marks": {
          ".read": true,
          "$key": {
            ".write": "!data.exists() && newData.val() === true"
          }
        },
        "ballots": {
          ".read": "root.child('h2/config/phase').val() !== 'VOTE'",
          "$id": {
            ".write": "!data.exists() && root.child('h2/config/phase').val() === 'VOTE'",
            ".validate": "newData.hasChildren(['first']) && newData.child('first').isString() && !newData.hasChild('ts')"
          }
        }
      },

      "pledges": {
        ".read": true,
        "$player": {
          ".write": true,
          ".validate": "newData.hasChildren(['text','ts']) && newData.child('text').isString() && newData.child('text').val().length <= 40"
        }
      },

      "draft": {
        ".read":  "auth != null && root.child('h2/coachUids/'+auth.uid).exists()",
        ".write": "auth != null && root.child('h2/coachUids/'+auth.uid).exists()"
      },

      "public": {
        ".read": true,
        "progress": { ".write": "auth != null && root.child('h2/coachUids/'+auth.uid).exists()" },
        "rosters": { ".write": "auth != null && root.child('h2/coachUids/'+auth.uid).exists()" },
        "power":   { ".write": "auth != null && root.child('h2/coachUids/'+auth.uid).exists()" },
        "winprob": { ".write": "auth != null && root.child('h2/coachUids/'+auth.uid).exists()" },

        "predict": {
          "marks":   { "$key": { ".write": "!data.exists() && newData.val() === true" } },
          "ballots": { "$id":  { ".write": "!data.exists()" } }
        },

        "comments": {
          "$team": {
            "$id": {
              ".write": "(!data.exists() && newData.exists()) || (auth != null && root.child('h2/coachUids/'+auth.uid).exists())",
              ".validate": "newData.hasChildren(['nick','text','ts']) && newData.child('text').isString() && newData.child('text').val().length <= 200 && newData.child('nick').val().length <= 10"
            }
          }
        }
      },

      "match": {
        ".read": true,
        ".write": "auth != null && root.child('h2/coachUids/'+auth.uid).exists()"
      }
    },

    "draftState": { ".read": true, ".write": false }
  }
}
```

**규칙 설계 포인트**
- `marks`는 부모에 `.read: true` → "18명 중 12명 투표 완료" 진행률 표시 가능. 선택 내용은 없으므로 안전.
- `!data.exists()` → **덮어쓰기 불가 = 중복 투표 DB 레벨 차단.** localStorage보다 훨씬 강함.
- `draft`는 인증된 코치 UID만. 선수는 `on('value')`를 걸어도 `PERMISSION_DENIED`.
- `pledges`는 의도적으로 `.write:true`(누구나, 이름 키로) — 이름이 곧 공개되는 자기소개 카드라 마크/발롯 분리가 필요 없다. 대신 `.validate`로 40자 제한.
- `public/progress`는 코치만 쓰기 가능, 픽이 확정될 때마다 `/h2/draft`와 같은 트랜잭션에서 `cursor` 값만 함께 갱신(§9.3).
- 댓글은 생성만 가능(수정·삭제 불가), 코치는 삭제 가능. 길이 검증도 규칙에서.
- 마지막 `draftState`는 상반기 데이터 읽기 전용 동결.

**적용 절차**
1. Firebase 콘솔 → Realtime Database → 규칙 탭에 붙여넣기 → 게시
2. Authentication → 로그인 방법 → **이메일/비밀번호 사용 설정**
3. 코치 7계정 생성 (`mh@fcpub.kr` … + `master@fcpub.kr`)
4. 각 계정 UID를 복사해 `/h2/coachUids`에 수동 입력 (콘솔에서 직접)
5. `?demo=1`로 선수 시점 접속 → `/h2/draft` 읽기가 실패하는지 **반드시 눈으로 확인**

---

## 8. 화면별 상세 명세

### 8.1 [요구사항 1] 선호팀 투표 — `index.html` @ `VOTE`

**흐름 (4탭이면 끝)**
```
① 이름 선택 (18명 그리드에서 내 이름 탭)
      ↓  이미 투표한 이름은 회색 처리 + "투표 완료" 뱃지
② 닉네임 입력 (선택, 미입력 시 "익명의 풉살러 #17" 자동 부여)
      ↓
③ 1순위 팀 선택 (필수) + 2순위 팀 선택 (선택)
      ↓
④ 각오 한마디 남기기 (**필수**, 40자 이내) — "뽑아주세요! 열심히 뛸게요 💪" 같은 자기소개
      ↓  ※ 예시 문구 칩을 눌러 넣고 고쳐 쓸 수 있다 (빈 칸 앞에서 막히지 않도록)
      ↓  ※ 이 항목만 이름이 공개된다 (§6.3) — ③까지는 완전 무기명
⑤ 제출 → "소중한 한 표 감사합니다 💚" + 진행률 (12/18)
```

**설계 결정**
- **로그인 없음.** 이름 선택이 곧 신원 확인. 18명 소규모 커뮤니티라 사회적 신뢰로 충분.
- **중복 방지 3중**:
  1. DB 규칙 `!data.exists()` — **핵심 방어** (기기 바꿔도 못 뚫음)
  2. `localStorage` — 재접속 시 즉시 "이미 투표하셨습니다" (UX용)
  3. 이름 그리드에서 투표 완료자 비활성화 — 시각적 안내
- **왜 localStorage만으로는 안 되는가**: 카톡 인앱 브라우저는 저장소가 격리·초기화될 수 있고, iOS Safari ITP는 7일 후 만료시킨다. **반드시 DB 마크가 주 방어선.**
- **이름 해시**: `marks` 키를 평문 이름 대신 짧은 해시로 저장하면 DB 스냅샷만 봤을 때 명단 노출이 줄어든다. (선택 사항 — 명단은 어차피 카톡에 공개되어 있으므로 평문도 무방)
- **각오 한마디**는 팀 선호도 투표와 별개로 `h2/pledges/{이름}`에 저장(§6.3) — 이름이 공개되는 것이 의도이므로 무기명 규칙(중복 방지 3중)이 적용되지 않는다. 나중에 다시 방문해 문구를 수정할 수 있도록 덮어쓰기를 허용.

**⚙️ 선택 강화 옵션 — 개인 PIN**
사칭 우려가 있다면: 코치가 사전에 18명에게 4자리 코드를 개별 전달(또는 이름별 고정 규칙) → 이름 선택 후 PIN 입력.
- 장점: 사칭 완전 차단
- 단점: 배포 비용 + "거창한 로그인 없이" 취지 훼손
- **권장: 기본은 PIN 없이 진행.** 18명 규모에서 남의 이름으로 투표할 동기가 거의 없고, 마찰이 참여율을 더 크게 해친다.

### 8.2 [요구사항 2] 프듀101 스타일 결과 공개 — `index.html` @ `REVEAL`

**집계 방식**
| 항목 | 점수 |
|---|---|
| 1순위 지목 | **3점** |
| 2순위 지목 | **1점** |

- 동점 시: ① 1순위 득표수 많은 팀 우선 → ② 그래도 같으면 화면상 랜덤 추첨 연출(시드 공개)
- 최종 산출: `draftOrder = [1위팀, 2위팀, 3위팀]` → **선호도 1위 팀이 지명 1번**

**연출 시나리오** (관리자가 `revealStep`을 0→1→2→3으로 올리면 전원 화면이 동시에 바뀜)
```
step 0  암전 + "2026 하반기 FC풉살 드래프트 순서 발표" + 카운트다운 3‧2‧1
step 1  🥉 3순위 지명권 ─ 팀 카드 슬라이드 인 + 득표수 0→N 카운트업
step 2  🥈 2순위 지명권 ─ 동일 연출
step 3  🥇 1순위 지명권 ─ 스포트라이트 + confetti + 팀 컬러 풀스크린 플래시
최종    "지명 순서: 1️⃣ ○○○팀  2️⃣ ○○○팀  3️⃣ ○○○팀"
```
- 전원이 카톡 링크로 동시에 보는 **라이브 이벤트**로 만드는 게 핵심. RTDB 실시간 동기화가 그대로 쓰인다.
- 개인별 투표 내역은 절대 표시하지 않음. **팀 득표 총합만.**

> 💡 **밸런스 옵션 (검토 요청)**: 지금 규칙은 "인기 1위 팀 = 지명 1번"입니다. 인기와 전력이 겹치면 격차가 커질 수 있어, 일반적인 드래프트는 역순(약팀 우선)을 씁니다.
> `config.orderMode: "popular" | "reverse"` 토글로 만들어 두고, 기본값은 요청대로 `popular`. 코치 회의에서 정하시면 한 줄로 전환됩니다.

### 8.3 [요구사항 3·4] 드래프트 보드 — `coach.html` @ `DRAFT`

**접근 제어**
- Firebase Auth 이메일/비밀번호 로그인. 미인증 상태에서는 화면 자체가 렌더링되지 않음.
- 선수가 URL을 알아내도 DB 읽기가 `PERMISSION_DENIED` → 빈 화면.
- 같은 시간 선수 화면(`index.html`)은 §8.3b의 비동기 대기 화면.

**진행 방식 — 3일간 비동기** (§1.5)
코치 6인이 한자리에 모이는 라이브 이벤트가 아니라, 9/11(금)~9/13(일) 18:00 사이에 각자 시간 날 때 접속해 자기 팀 차례를 처리하는 방식이다. 한 팀이 픽을 마치면 **사이트가 아니라 카톡방에서 직접** 다음 팀에게 알린다("저희 A팀 뽑기 끝났습니다, B팀 코치님 부탁드려요 🙏"). 그래서 이 화면은 "지금 당장 눌러야 하는" 긴박한 타이머 UI가 아니라, 접속했을 때 "지금 내 차례인지 한눈에" 보이는 것이 핵심이다.

**스네이크 순서 (18픽)** — `draftOrder = [X, Y, Z]` 기준
```
R1  X  Y  Z        픽  1  2  3
R2  Z  Y  X        픽  4  5  6
R3  X  Y  Z        픽  7  8  9
R4  Z  Y  X        픽 10 11 12
R5  X  Y  Z        픽 13 14 15
R6  Z  Y  X        픽 16 17 18
```
**공정성 검증**: 픽 번호 합계 X = 1+6+7+12+13+18 = **57**, Y = 2+5+8+11+14+17 = **57**, Z = 3+4+9+10+15+16 = **57**.
→ 세 팀 완전 동일. 상반기 `[1,2,3,3,2,1,...]` 패턴을 6라운드로 그대로 축소 적용.

**화면 구성**
```
┌─────────────────────────────────────────┐
│ 🔴 현재 턴: 민호경표팀  (7픽 / 18)          │
│ 순서: [A]▶[B]▶[C]▶[C]▶[B]▶[A]            │
├──────────────────┬──────────────────────┤
│  선수 풀 (남은 N명) │  A 민호경표팀 ●●●○○○  │
│  ┌────┐┌────┐    │  B 원배원언팀 ●●○○○○  │
│  │가은││나영│ ... │  C 경목준하팀 ●●○○○○  │
│  │👀🔥││    │    │                      │
│  │💬"열심히..." │  │  [픽 로그]            │
│  └────┘└────┘    │                      │
├──────────────────┴──────────────────────┤
│ [ 지명 확정 ]  [ 방금 픽 취소 ]            │
│ [ 📋 다음 팀 알림 문구 복사 ]              │
└─────────────────────────────────────────┘
```
- **찜(👀) / 원픽(🔥)**: 상반기 기능 그대로 계승. 코치 이름 뱃지로 표시되어 눈치싸움 유발.
- **각오 한마디 말풍선(💬)**: `h2/pledges`에 선수가 남긴 문구가 있으면, 선수 카드 아래 도트 감성 2등신 캐릭터가 말하는 듯한 말풍선으로 노출. 참고용일 뿐 픽을 강제하지 않는다 (§6.3).
- **픽 타이머는 두지 않는다.** 라이브로 한자리에 모이는 게 아니라 비동기 진행이므로 강제 시간 제한이 의미가 없다(§14 #6). 대신 픽 확정 직후 **"다음 팀 알림 문구 복사"** 버튼으로 카톡에 붙여넣을 문구(예: "B팀 코치님, 저희 A팀 뽑기 끝났습니다! 이제 B팀 차례예요 🙏")를 자동 생성 — 알림 자체는 여전히 코치가 카톡으로 수동 전송.
- **픽 로그**: 코치 화면에서만. `#7 민호경표팀 ← 해수`.
- ⛔ **상반기 "Hype 랭킹(선수별 점수)" 위젯은 삭제.** 원칙 2 위반.

### 8.3b [신규] 선수 대기 화면 — `index.html` @ `DRAFT`
드래프트가 3일에 걸쳐 비동기로 진행되는 동안, 선수들이 심심하지 않도록 만드는 화면. 실제 드래프트 보드는 절대 보여주지 않되(§7), 아래 두 가지만 공개 데이터(`h2/public/progress`, `h2/pledges`)로 안전하게 보여준다.

```
┌─────────────────────────────────┐
│   🔒 지금 코치들이 팀을 고르고 있어요  │
│                                   │
│   ████████░░░░░░░░  6 / 18        │  ← public/progress.cursor (§6.4)
│   지금은 원배원언팀 차례예요           │  ← draftOrder(이미 공개) + cursor로 계산
│                                   │
│   ─── 선수들의 각오 한마디 ───        │
│   💬 나영 "열심히 뛸게요!"            │
│   💬 해수 "이번엔 진짜 잘할 수 있어요"  │  ← h2/pledges, 가나다순/랜덤 노출
│   ...                             │
└─────────────────────────────────┘
```
- 진행률 숫자와 "지금 ○○팀 차례" 문구만 노출 — **누가 뽑혔는지, 어느 순서로 뽑혔는지는 절대 노출하지 않는다.**
- "지금 ○○팀 차례"는 REVEAL에서 이미 전원에게 공개된 `draftOrder`와 `progress.cursor`만으로 클라이언트에서 계산 — 새로운 비공개 정보를 노출하는 게 아니다(§6.4).
- 각오 한마디 목록은 매 방문마다 순서를 섞어(또는 가나다순 고정) 노출해 특정 선수가 눈에 띄지 않게 한다.

### 8.4 [요구사항 5] 최종 명단 공개 — `index.html` @ `RESULT`

**순서 은닉 처리** (코치 앱이 `status: done` 전환 시 1회 실행)
```js
// 1) 픽 순서를 팀별로 모은 뒤
const raw = { A: [...], B: [...], C: [...] };          // 지명 순서 그대로 (내부용)
// 2) Fisher-Yates 셔플 → 3) 가나다순 정렬 → 4) 공개 노드에 기록
const publish = t => raw[t].slice()
  .map(v => [Math.random(), v]).sort((a,b) => a[0]-b[0]).map(x => x[1])   // 셔플
  .sort((a, b) => a.localeCompare(b, 'ko'));                              // 가나다순 확정
db.ref('h2/public/rosters').set({ A: publish('A'), B: publish('B'), C: publish('C') });
```
> **셔플 + 가나다순 정렬을 둘 다 하는 이유**: 셔플만 하면 "혹시 첫 번째가 1픽 아냐?"라는 심리적 추측이 계속 생깁니다.
> 가나다순으로 확정 노출하면 **순서에서 읽어낼 정보가 0이라는 게 자명해져서** 추측 자체가 사라집니다. 이게 원칙 3의 취지에 더 부합합니다.
> (셔플을 앞에 두는 건 만에 하나 정렬이 안정 정렬로 원본 순서를 남기는 경우까지 차단하기 위한 이중 안전장치)

**공개 연출**: 3팀 카드 순차 등장 + confetti (상반기 `.final-view` 스타일 확장) + "우리 팀 찾기" 하이라이트.

### 8.5 [요구사항 6] 순위 예측 — `index.html` @ `PREDICT`

- 3팀 카드를 **드래그 또는 탭으로 1·2·3위 배치**
- 투표와 동일한 `marks`/`ballots` 분리 구조 → 무기명 + 중복 방지
- 제출 후 전체 예측 분포 공개: "68%가 민호경표팀 우승 예상"
- **마감**: 매치 당일(12/19) 킥오프 전 `MATCH` phase 전환 시 자동 잠금
- 매치 종료 후 **적중자 닉네임 발표** — 3개월 하이프의 피날레

### 8.6 [요구사항 7] 팀별 우승 확률 시각화 — `index.html` @ `PREDICT`

**⚠️ 설계 제약**: 원칙 2에 따라 **개인 평점을 절대 만들지 않는다.** 따라서 승률은 아래 두 축의 팀 단위 집계값으로만 산출한다.

| 축 | 가중치 | 산출 |
|---|---|---|
| **팬심 지수** | 60% | 예측 투표에서 해당 팀을 1위로 꼽은 비율 |
| **전력 지수** | 40% | 팀이 확보한 픽 순번 가치 합 = `Σ 0.93^(픽번호-1)`, 정규화 |

**전력 지수 계산 주체가 중요**: 픽 순번은 비공개이므로, **코치 앱이 계산해서 `power` 집계값 하나만** `/public/power/{팀}`에 기록한다. 개별 픽 정보는 공개 노드에 절대 나가지 않는다.
> 참고: 스네이크 특성상 세 팀 픽 가치 합이 거의 같아(§8.3 검증) 전력 지수 차이는 미미하다. 실질적으로 팬심이 승률을 좌우하며, 이게 오히려 **"실력 평가가 아닌 분위기 지표"**라는 점에서 감정적으로 안전하다.

**우승 확률 산출**: 라운드로빈(A-B, B-C, C-A) **몬테카를로 10,000회 시뮬레이션**
```
각 경기 승률 = sigmoid(k × (전력_home − 전력_away)),  무승부 확률 별도 반영
승점(승 3 / 무 1 / 패 0) 집계 → 1위 횟수 / 10,000 = 우승 확률
```

**시각화** (다크 배경 + 팀 컬러)
- 도넛 차트 3분할 (우승 확률)
- 가로 스택 바 (팬심 / 전력 기여도 분해)
- 숫자 카운트업 애니메이션
- 12월 매치 당일에는 경기 결과를 반영해 **실시간 재계산**

### 8.7 [요구사항 8] 팀별 댓글 — `index.html` @ `PREDICT`

- 팀 3탭 전환. 닉네임(2~10자) + 본문(200자).
- 저장: `/public/comments/{팀}/{pushId}` — 여기선 시간순 정렬이 필요하므로 `push()` 사용 OK (무기명 요구가 없는 영역).
- **모더레이션**: 규칙상 수정·삭제 불가(작성자도) / 코치·마스터만 삭제. `admin.html`에 최근 댓글 목록 + 삭제 버튼.
- **선플 가이드** 상단 고정: `"팀 응원과 도발은 환영! 개인을 향한 실력 평가는 삼가주세요 💚"`
- 금칙어 필터(클라이언트) + 연속 게시 10초 쿨다운(localStorage).
- 이모지 리액션 👍🔥😂 (선택 구현)

---

## 9. 드래프트 로직 상세

### 9.1 순서 확정
```js
score[t] = 3 × (t를 1순위로 고른 수) + 1 × (t를 2순위로 고른 수)
draftOrder = 팀 3개를 score 내림차순 정렬   // 동점 → 1순위 득표수 → 랜덤
```

### 9.2 스네이크 시퀀스 생성
```js
const seq = [];
for (let r = 0; r < 6; r++) {
  const round = (r % 2 === 0) ? draftOrder : [...draftOrder].reverse();
  seq.push(...round);
}   // 길이 18
```

### 9.3 동시성 — 상반기 최대 버그 수정
상반기: `db.ref('draftState').set(state)` → 코치 2명이 동시에 누르면 나중 쓰기가 앞 픽을 **통째로 덮어씀**.

이번:
```js
db.ref('h2/draft').transaction(cur => {
  if (!cur) return cur;
  if (cur.status === 'done') return;                     // 중단
  const seq = buildSeq(order);
  if (seq[cur.cursor] !== myTeam) return;                // 내 턴 아님 → 롤백
  if ((cur.picks || []).some(p => p.player === player)) return;  // 이미 지명됨
  cur.picks = [...(cur.picks || []), { seq: cur.cursor, team: myTeam, player, by: myId, ts: Date.now() }];
  cur.cursor += 1;
  if (cur.cursor === 18) cur.status = 'done';
  return cur;
});
// 같은 트랜잭션 성공 콜백 안에서 공개 진행률도 함께 갱신 — 두 값이 어긋나지 않게
db.ref('h2/public/progress/cursor').set(newCursor);
```
→ 턴 검증·중복 검증·커서 증가가 **하나의 원자적 연산**. 동시 클릭해도 한 명만 성공. 성공 직후 `public/progress`도 갱신해 선수 대기 화면(§8.3b)의 진행률이 실시간으로 따라간다.

### 9.6 턴 핸드오프 — 카톡 수동 알림
코치 6인이 동시에 접속해 있지 않으므로, "내 턴이다"를 알리는 주체는 시스템이 아니라 **직전 팀 코치**다. 픽 확정 버튼을 누르면 코치 화면에 "다음 팀 알림 문구 복사" 버튼이 뜨고(§8.3), 코치가 그 문구를 그대로 카톡 코치방에 붙여넣어 다음 팀에게 알린다. 사이트는 알림을 발송하지 않으며, 이는 의도된 설계다 — 코치 6인이 항상 대기하고 있는 게 아니라 각자 바쁜 틈에 접속하는 상황에서, 서버 푸시 알림 인프라를 새로 만드는 것보다 이미 쓰고 있는 단체 카톡방을 그대로 활용하는 편이 훨씬 안정적이다.

### 9.4 픽 취소 (Undo)
- **직전 1픽만** 취소 가능, 취소 권한은 **해당 픽을 한 코치 본인 + 마스터**.
- 다음 픽이 이미 진행되면 코치는 불가, 마스터만 강제 롤백.
- 취소 시 `picks.pop()` + `cursor -= 1` 을 역시 트랜잭션으로.
- `status: done` 이후에는 **취소 불가** (이미 공개 명단이 기록되었으므로). 되돌리려면 마스터가 `/public/rosters` 삭제 후 재실행.

### 9.5 완료 처리 (원자적으로 묶을 것)
```
cursor === 18  →  status = 'done'
               →  publish(rosters)   // 가나다순
               →  publish(power)     // 팀 집계값
               →  config.phase = 'RESULT'
```
- 이 4개는 **마스터가 "결과 공개" 버튼을 눌러 한 번에** 실행. 자동 전환하지 않는다.
  (드래프트 직후 코치들이 명단을 한 번 눈으로 검수할 시간을 확보 — 오지명 최종 방어선)

---

## 10. 개발 일정

**전략: "이번 주 필수(MVP)"와 "12월까지 여유분"을 명확히 자른다.**
드래프트 D-day에 반드시 있어야 하는 건 Phase 0~3뿐. 예측·승률·댓글은 3개월간 천천히 붙여도 된다.

### 🔥 1차 — 드래프트 마감까지 (09/08 화 밤 ~ 09/13 일 18:00)

투표 오픈이 **9/9(수) 오전 10시**로 확정되면서 개발 착수부터 첫 오픈까지 하루가 채 안 남는다. P0·P1은 오늘(9/8) 밤 안에 끝내야 한다.

| Phase | 작업 | 산출물 | 마감 |
|---|---|---|---|
| **P0** | Firebase 설정 (Auth 활성화, 코치 7계정, `coachUids` 입력), **보안 규칙 배포 및 침투 검증**, 저장소 구조 개편, 상반기 파일 `archive/` 이동 | `database.rules.json`, `assets/config.js` | 09/08 (화) 밤 |
| **P1** | 선수 투표 화면 (이름선택 → 닉네임 → 1·2순위 → 각오 한마디 → 제출), 중복 방지 3중, 진행률 | `index.html` @VOTE | 09/08 (화) 밤 — **09/09 (수) 10:00 오픈 전 완료 필수** |
| **P2** | 프듀101 발표 화면 + `revealStep` 실시간 동기화 + 카운트업/confetti | `index.html` @REVEAL, `admin.html` | 09/10 (목) |
| **P3** | 코치 드래프트 보드 (Auth 게이트, 스네이크, 찜/원픽, 각오 한마디 말풍선, 트랜잭션, Undo, 픽 로그, 턴 알림 문구 복사) + 선수 대기 화면(§8.3b) | `coach.html`, `index.html` @DRAFT | 09/10 (목) |
| **P3.5** | 최종 명단 공개 화면 (가나다순 + 연출) — 요구사항 5 | `index.html` @RESULT | 09/10 (목) |
| **QA** | 6명 코치 모의 드래프트 리허설 + 모바일/카톡 인앱 브라우저 테스트 | `docs/QA.md` 체크 | 09/10~09/11 (금) 오전 |

**→ 09/9(수) 10:00 투표 오픈 → 09/10(목) 22:00 투표 마감 → 09/11(금) 오전 리허설 통과 후 본 드래프트 창(§1.5) 오픈 → 09/13(일) 18:00 마감**

> P0·P1이 촉박하면 최소한 "투표 화면 + 중복 방지 규칙"만 먼저 배포하고, 각오 한마디 입력·발표 연출은 9/10에 순차 배포해도 무방하다 — 투표 자체가 막히는 것만 피하면 된다.

### 🌱 2차 — 12월 매치까지 (09/14 ~ 12/18) · 여유롭게

| Phase | 작업 | 소요 | 목표 |
|---|---|---|---|
| **P4** | 순위 예측 화면 + 분포 시각화 (요구사항 6) | 1일 | 9월 말 |
| **P5** | 승률 엔진 (몬테카를로) + 도넛/스택바 시각화 (요구사항 7) | 1일 | 10월 초 |
| **P6** | 팀별 댓글 + 모더레이션 + 선플 가이드 (요구사항 8) | 0.5일 | 10월 중 |
| **P7** | 매치데이 모드 — 경기 결과 입력, 실시간 순위표, 예측 적중자 발표 | 1일 | 12월 중순 |
| **P8** | 하이프 콘텐츠 — D-day 카운트다운, 팀 슬로건, 팀별 대표 이미지 | 0.5일 | 12월 중순 |

**총 개발량 ≈ 8인일** (1차 4.5일 + 2차 4일)

---

## 11. 운영 시나리오 (D-day 매뉴얼 초안)

### 11.1 타임라인
드래프트가 하루 저녁 라이브 이벤트가 아니라 **3일짜리 비동기 창**이라는 점이 상반기와 가장 다르다(§1.5). 코치 6인이 각자 시간 날 때 접속해 픽하고, 팀이 끝나면 카톡으로 직접 다음 팀을 부른다 — 사이트는 그 알림을 대신 보내주지 않는다.

| 시점 | 담당 | 액션 |
|---|---|---|
| 09/09 (수) 10:00 | 마스터 | `phase = VOTE` → 카톡 공지 + 링크 (각오 한마디 포함) |
| 09/09~09/10 | — | 미투표자 리마인드 (진행률 화면 공유) |
| 09/10 (목) 22:00 | 마스터 | `phase = REVEAL` (투표 자동 마감) |
| 09/11 (금) 오전 | 마스터 | 카톡 "결과 나왔어요!" → `revealStep` 0→1→2→3 순차 공개, 지명 순서 확정 |
| 09/11 (금) 오전 | 마스터 | `phase = DRAFT` — 드래프트 창 오픈, 선수는 §8.3b 대기 화면 |
| 09/11 (금) ~ 09/13 (일) 18:00 | 코치 6인 | `coach.html`에서 각자 시간 날 때 접속해 18픽 진행. 팀이 끝나면 **카톡으로 다음 팀 직접 호출**(§9.6) |
| 09/13 (일) 18:00 이후 | 코치 6인 | 명단 육안 검수 |
| 09/13 (일) 저녁 | 마스터 | **"결과 공개"** → `RESULT` (명단 + confetti) |
| 09/13 (일) 밤 | 마스터 | `phase = PREDICT` → 예측·승률·댓글 오픈 |
| 12/19 (토) | 마스터 | `phase = MATCH` → 예측 잠금, 결과 입력 |

> **혹시 18픽이 일요일 18시까지 안 끝나면?** 마스터가 코치방에 남은 팀을 태그해 마감을 안내하고, 필요하면 창을 몇 시간 연장한다 — 강제 자동 마감은 하지 않는다(§13 리스크).

### 11.2 카톡 공지 문구 초안

**① 투표 오픈** — 투표가 드래프트 순서를 어떻게 정하는지 최대한 친절하고 자세하게 풀어씀

```
📢 FC풉살 하반기 내부매치 드래프트 안내

12월 내부매치 팀을 나눌 드래프트를 시작합니다!
이번엔 특별하게, 여러분이 가고 싶은 팀에 투표한 결과가
드래프트 순서를 결정해요 ⚽

👉 [링크]

투표는 이렇게 진행돼요
· 내 이름을 선택하고, 가고 싶은 팀을 1순위 · 2순위로 골라주세요
· 완전 무기명이에요! 누가 어느 팀에 투표했는지는
  코치들도, 저희도 확인할 수 없어요
· 로그인 없이 이름만 누르면 끝, 1분이면 충분해요
· 한 사람당 딱 한 번만 투표할 수 있어요 (중복 투표 불가)

투표 결과, 우리 팀을 가장 많이 원한 코치들이
가장 먼저 선수를 뽑을 기회를 갖게 돼요.
1등 팀이 드래프트 1번, 2등 팀이 2번, 3등 팀이 3번 순서로 진행됩니다.

투표할 때 "각오 한마디"를 딱 한 줄 남겨주세요 (40자).
코치님들이 드래프트하는 내내 이 한 줄을 보면서 선수를 고릅니다.
나를 어필할 수 있는 유일한 칸이니 꼭 채워주세요 🔥
(예시 문구도 준비해뒀으니 눌러서 고쳐 쓰셔도 돼요)

⏰ 마감: 9/10(목) 밤 10시!
결과는 9/11(금)에 프로듀스101처럼 재미있게 공개할게요 👀
```

**② 투표 마감 · 결과 공개**
```
🎬 투표 마감! 결과가 궁금하시죠?

👉 [링크]

투표해주신 결과로 드래프트 순서가 정해졌어요.
지금부터 일요일(9/13) 저녁 6시까지, 코치들이 각자 시간 날 때
순서대로 드래프트를 진행합니다.

드래프트는 코치들끼리만 비공개로 진행돼요 — 과정은 안 보이지만,
링크에 들어오면 지금 몇 명이 확정됐는지, 어느 팀 차례인지,
그리고 다른 선수들이 남긴 각오 한마디를 구경할 수 있어요 💬

결과가 다 나오면 짜잔 하고 팀 명단을 공개할게요, 조금만 기다려주세요!
```

**③ 결과 공개**
```
🎉 2026 하반기 FC풉살 팀 확정!

👉 [링크]

※ 팀 명단은 가나다순입니다. 지명 순서는 코치들만 알고 있고
   어디에도 공개되지 않으니 편하게 즐겨주세요 💚

이제 우승 예측과 팀 응원 댓글이 열렸습니다.
12/19 매치까지 3개월, 뜨겁게 가봅시다 🔥
```

**④ [코치 전용] 턴 핸드오프 템플릿** — 코치 카톡방에서 사용, 픽 확정 시 사이트가 문구를 자동 생성(§8.3)
```
저희 A팀 뽑기 끝났습니다! 이제 B팀 차례예요, 코치님 부탁드려요 🙏
```

### 11.3 오프라인 백업 플랜
- 드래프트 중 네트워크 장애 시: **종이 픽 시트**를 미리 인쇄해 두고 수기 진행 → 이후 마스터가 `admin.html`에서 일괄 입력.
- 코치 로그인 실패 대비: 마스터 계정으로 대리 픽 가능한 "대행 모드" (팀 선택 드롭다운).
- 코치가 카톡으로 다음 팀을 부르는 걸 깜빡할 수 있으므로, 마스터가 하루 한 번(오전/저녁) `public/progress`를 확인해 진행이 멈춰 있으면 코치방에 직접 리마인드.

---

## 12. QA 체크리스트 (`docs/QA.md`)

### 보안 (최우선)
- [ ] 로그아웃 상태에서 콘솔로 `firebase.database().ref('h2/draft').once('value')` → **PERMISSION_DENIED** 확인
- [ ] 선수 화면 네트워크 탭에 지명 순서 관련 페이로드가 **일절 없음** 확인
- [ ] `/public/rosters`가 드래프트 **완료 전에는 존재하지 않음** 확인
- [ ] 투표 중 `h2/vote/ballots` 읽기 차단 확인
- [ ] 같은 이름으로 재투표 시도 → 규칙에서 거부 확인
- [ ] 시크릿 창 / 다른 기기에서도 중복 투표 차단 확인 (localStorage 우회 테스트)
- [ ] 선수 대기 화면(§8.3b)의 네트워크 탭에 `h2/pledges`, `h2/public/progress` 외 다른 페이로드 없음 확인

### 기능
- [ ] 코치 2명 동시 픽 클릭 → 한 명만 성공, 픽 유실 없음
- [ ] Undo 후 커서·명단·`public/progress` 정합성
- [ ] 18픽 완료 시 각 팀 정확히 6명
- [ ] 선수 18명 전원이 어느 한 팀에 정확히 1번 배정
- [ ] `revealStep` 변경이 모든 접속 기기에 2초 내 반영
- [ ] 각오 한마디 40자 제한 및 재제출(덮어쓰기) 동작 확인
- [ ] "다음 팀 알림 문구 복사" 버튼이 픽 확정 직후 정확한 다음 팀명으로 생성되는지 확인
- [ ] 코치가 며칠 걸쳐 로그인·로그아웃을 반복해도 드래프트 진행 상태가 유지되는지 확인 (비동기 진행 특성)

### 환경 (실기기 필수)
- [ ] **카카오톡 인앱 브라우저** (iOS/Android 둘 다) — 링크 진입, 투표, localStorage 동작
- [ ] iOS Safari / Android Chrome
- [ ] 작은 화면(iPhone SE 375px)에서 18명 그리드 레이아웃 깨짐 없음
- [ ] 다크 모드 강제 적용 브라우저에서 가독성

### 부하
- [ ] 18명 동시 접속 시 RTDB 동시 연결 정상 (Spark 무료 티어 한도 100 — 충분)

---

## 13. 리스크 & 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 🔴 **지명 순번 유출** | 프로젝트 실패 (감정 사고) | 보안 규칙 + 공개/비공개 노드 분리 + QA 침투 테스트 필수 통과 |
| 🔴 카톡 인앱 브라우저에서 Firebase 미동작 | 투표 자체 불가 | **P1 완료 즉시** 실기기 검증. 실패 시 "외부 브라우저로 열기" 안내 배너 |
| 🟠 투표 참여율 저조 | 순서 결정 근거 빈약 | 진행률 공개 + 리마인드. 12명 미만이면 코치 회의로 순서 결정(폴백 규칙 사전 합의) |
| 🟠 동시 픽 충돌로 픽 유실 | 드래프트 중단 | 트랜잭션 적용(§9.3) + Undo + 픽 로그 |
| 🟠 비동기 진행 중 코치가 카톡 알림을 깜빡함 | 드래프트가 며칠간 멈춰 있을 수 있음 | 진행률 문구 자동 생성(§8.3, §9.6) + 마스터의 하루 1회 진행 상황 확인·리마인드(§11.3) |
| 🟠 9~12월 3개월간 Firebase 유휴 | 프로젝트 비활성 경고 | 월 1회 접속 확인. 12/1에 전체 동작 재점검 일정 고정 |
| 🟡 사칭 투표 | 결과 왜곡 | 18명 소규모라 위험 낮음. 필요 시 PIN 옵션 활성화 |
| 🟡 댓글 과열 | 감정 사고 | 선플 가이드 + 코치 삭제권 + 금칙어 필터 |
| 🟡 명단 확정 후 참가 인원 변동 | 팀 불균형 | 대체 선수 규칙 사전 합의 (동일 팀 자리 승계 / 재드래프트 없음) |

---

## 14. 결정이 필요한 항목

| # | 항목 | 선택지 | 권장 |
|---|---|---|---|
| 1 | 투표 방식 | (a) 1순위만 / (b) **1순위+2순위(3점·1점)** | **(b)** — 변별력이 생기고 동점 확률이 크게 줄어듦 |
| 2 | 지명 순서 | (a) **인기 1위 → 지명 1번** / (b) 역순(밸런스) | **(a)** — 요청대로. 단 `orderMode` 토글로 만들어 코치 회의 후 전환 가능하게 |
| 3 | 중복 방지 강도 | (a) **이름 선택만** / (b) 개인 PIN 추가 | **(a)** — 마찰 최소화. DB 규칙이 기기 우회를 이미 막음 |
| 4 | 공개 명단 정렬 | (a) 랜덤 셔플 / (b) **가나다순** | **(b)** — 순서 추측 자체를 원천 차단 (원칙 3에 더 부합) |
| 5 | 승률 산출 | (a) **팬심 60% + 전력 40%** / (b) 팬심 100% | **(a)** — 단, 개인 평점은 절대 만들지 않음 |
| 6 | 픽 타이머 | (a) 60초 안내용 / (b) **없음 + 카톡 수동 턴 알림** / (c) 강제 자동픽 | **(b)** — 코치 6인이 한자리에 모이지 못해 3일 비동기로 진행하므로(§1.5) 시간 제한 자체가 무의미. 대신 "다음 팀 알림 문구 복사" 버튼으로 카톡 전달을 돕는다(§8.3, §9.6) |
| 7 | 상반기 파일 | (a) **`archive/2026-h1/`로 이동** / (b) 삭제 | **(a)** — 기록 보존 |
| 8 | 백엔드 플랫폼 (§5.1a) | (a) Firebase RTDB (계승) / (b) **Supabase (신규)** | ✅ **확정 (b)** — 기존 활성 프로젝트에 `draft_` 접두사로 구축 완료. 콘솔 수작업 없이 스키마·RLS·함수까지 전부 코드로 배포됨 |
| 9 | 코치 로그인 (§5.1b) | (a) **하드코딩 암호(강화판)** / (b) 이메일 매직링크 | ✅ **확정 (a)** — 8자리 랜덤 암호를 DB에만 저장. 소스에는 없고, 암호 검증은 서버 함수 안에서만 이뤄짐 |
| 10 | 화려한 UI 착수 시점 | (a) 지금 바로 착수 / (b) 디자인 참고자료 수신 후 | ✅ **확정 (a)** — 참고자료를 못 찾아 바로 진행. **슬랙(Slack) 스타일**로 확정 (§5.4) |
| 11 | 각오 한마디 무기명 여부 | (a) 무기명 / (b) **이름 공개** | **(b)** — "나를 뽑아달라"는 자기소개가 목적이라 실명이 전제. 팀 선호도 투표(무기명)와는 완전히 분리된 노드(§6.3) |

---

## 15. 작업 티켓 목록

> 아래 T-01~T-03은 **Firebase 기준**으로 적어둔 것. §14 #8에서 Supabase로 확정되면
> "Auth 계정 생성" → "Supabase 프로젝트 + Auth 설정(MCP로 직접 실행)", "RTDB 규칙" → "RLS 정책(SQL)"로 1:1 치환되며,
> 오히려 사람이 콘솔에서 손댈 일이 줄어드는 방향이라 티켓 개수는 비슷하거나 더 적어질 것으로 예상.

### 1차 (D-day 필수)
- [ ] **T-00** 백엔드 플랫폼 최종 확정 (Firebase vs Supabase, §14 #8) — 디자인 참고자료 받을 때 함께 컨펌
- [ ] **T-01** Auth 설정 + 코치 계정/보강된 하드코딩 암호 발급 (§5.1b)
- [ ] **T-02** `database.rules.json`(Firebase) 또는 RLS 정책(Supabase) 작성 및 배포
- [ ] **T-03** 코치 식별자 매핑 등록 (Firebase: UID 수동 입력 / Supabase: 이 세션에서 MCP로 직접 시딩)
- [ ] **T-04** 보안 침투 테스트 (§12 보안 항목 전체)
- [ ] **T-05** 저장소 구조 개편 + 상반기 파일 `archive/2026-h1/` 이동
- [ ] **T-06** `assets/config.js` — 팀/선수/코치 단일 소스
- [ ] **T-07** `assets/app.js` — firebase init, phase 구독, 공통 유틸
- [ ] **T-08** `index.html` @VOTE — 이름선택 · 닉네임 · 1·2순위 · 각오 한마디(선택) · 제출 · 중복 방지 3중
- [ ] **T-08b** `h2/pledges` 노드 + 각오 한마디 저장 로직 (§6.3) — 무기명 규칙 미적용, 40자 검증
- [ ] **T-09** 투표 진행률 표시 (n/18)
- [ ] **T-10** `admin.html` — phase 전환 + `revealStep` 컨트롤러 + 리셋
- [ ] **T-11** `index.html` @REVEAL — 프듀101 연출 (카운트다운·카운트업·confetti)
- [ ] **T-12** 득표 집계 + `draftOrder` 확정 로직 (동점 처리 포함)
- [ ] **T-13** `coach.html` — Auth 게이트 + 로그인 UI
- [ ] **T-14** 드래프트 보드 렌더 (턴 인디케이터 · 시퀀스 바 · 선수 풀 · 3팀 로스터 · 각오 한마디 말풍선)
- [ ] **T-15** 찜(👀) / 원픽(🔥) 기능 이식
- [ ] **T-16** 픽 확정 트랜잭션 (§9.3) — 성공 시 `public/progress.cursor` 동시 갱신
- [ ] **T-17** Undo + 픽 로그
- [ ] **T-18** "다음 팀 알림 문구 복사" 버튼 (§8.3, §9.6) — 픽 타이머 대신 카톡 수동 턴 알림 지원
- [ ] **T-19** `index.html` @DRAFT — 선수 대기 화면(§8.3b): 진행률 + 현재 팀 차례 + 각오 한마디 게시판
- [ ] **T-20** 완료 처리 — 셔플+정렬 후 `/public/rosters` 1회 기록 (§9.5)
- [ ] **T-21** `index.html` @RESULT — 3팀 명단 공개 연출
- [ ] **T-22** `docs/OPERATIONS.md` 운영 매뉴얼 + 카톡 공지 문구
- [ ] **T-23** `docs/QA.md` + 6인 모의 드래프트 리허설
- [ ] **T-24** 모바일 · 카톡 인앱 브라우저 실기기 검증

### 2차 (12월까지)
- [ ] **T-25** `index.html` @PREDICT — 1·2·3위 예측 UI + 중복 방지
- [ ] **T-26** 예측 분포 시각화
- [ ] **T-27** 전력 지수 계산 + `/public/power` 기록 (코치 앱)
- [ ] **T-28** 몬테카를로 라운드로빈 승률 엔진
- [ ] **T-29** 승률 시각화 (도넛 + 스택바 + 카운트업)
- [ ] **T-30** 팀별 댓글 (작성 · 목록 · 쿨다운 · 금칙어)
- [ ] **T-31** 댓글 모더레이션 (`admin.html` 삭제 기능)
- [ ] **T-32** 매치데이 — 결과 입력 · 실시간 순위표
- [ ] **T-33** 예측 적중자 발표
- [ ] **T-34** D-day 카운트다운 · 팀 슬로건 등 하이프 콘텐츠

---

## 16. 요구사항 ↔ 구현 대응표

| 요구사항 | 구현 위치 | 티켓 |
|---|---|---|
| 1. 무기명 선호팀 투표, 로그인 X, 중복 X | §8.1, §6.2, §7 | T-08, T-09, T-02 |
| 2. 프듀101식 결과 공개 | §8.2 | T-11, T-12 |
| 3. 정해진 순번대로 코치 드래프트 | §8.3, §9.2 | T-14, T-16 |
| 4. 코치만 진행, 선수는 볼 수 없음 | §7 (보안 규칙), §8.3, §8.3b | T-02, T-04, T-13, T-19 |
| (신규) 선수 각오 한마디 | §6.3, §8.1, §8.3, §8.3b | T-08b, T-14, T-19 |
| 5. 팀 내 지명 순번 은닉 공개 | §6.1, §8.4, §9.5 | T-20, T-21 |
| 6. 1·2·3위 예측 | §8.5 | T-25, T-26 |
| 7. 팀별 우승 확률 시각화 | §8.6 | T-27, T-28, T-29 |
| 8. 팀별 댓글 | §8.7 | T-30, T-31 |
